{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "ava": "^0.25.0",
    "eslint-config-prettier": "^2.9.0",
    "husky": "^0.14.3",
    "lint-staged": "^6.1.0",
    "nyc": "^13.0.1",
    "prettier": "^1.10.2",
    "xo": "^0.18.2"
  },
  "scripts": {
    "precommit": "lint-staged",
    "lint": "xo",
    "test": "npm run lint && NODE_ENV=test npx nyc ava"
  },
  "xo": {
    "extends": "prettier"
  },
  "lint-staged": {
    "*.js": [
      "npm run lint",
      "prettier --single-quote --no-semi --write",
      "git add"
    ]
  }
}
